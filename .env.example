# news-collector 运行时密钥
#
# 用法：把本文件复制为 ~/.news-collector/.env 后填入真实值：
#   mkdir -p ~/.news-collector
#   cp .env.example ~/.news-collector/.env
#   # 然后编辑 ~/.news-collector/.env 填入真实 token
#
# 注：本 .env 由 docker-compose.yml 通过 env_file 读取并注入 RSSHub 容器；
#     news-collector Python 进程当前不读取任何密钥（采集层无 LLM 调用）。

# X (Twitter) 小号 cookie 中的 auth_token 字段（约 40 位 hex 字符串）
# 提取步骤详见 docs/rsshub-setup.md §3
# 多个 token 用逗号分隔可轮询，提升稳定性：
#   TWITTER_AUTH_TOKEN=token1,token2,token3
TWITTER_AUTH_TOKEN=
